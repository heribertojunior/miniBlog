<%- include("partials/header") -%>
    <h1>Home</h1>
    <p><%= homecontent %></p>
    <% post.forEach((item, i) => {%>
      <h1><%= item.title %></h1>
      <p><%= item.compose.substr(0, 100)+"..." %><a href = "<%= "/posts/"+item.title.replace(" ","-") %>">Read more</a></p>
    <%   }); %>
<%- include("partials/footer") -%>
